using System.Collections;
using System.Collections.Generic;
using UnityEngine;

namespace Project.Core
{
    public enum CoordinatesTypes
    {
        Local, Global
    }

    public enum AxisType
    {
        OX, OY
    }

    public enum CoordinatesPlaneType
    {
        XOY, XOZ, YOZ
    }

    public enum DelayType
    {
        Default, Realtime
    }
}